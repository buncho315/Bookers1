<body>
  <p id="notice"></p>
  <h1>Books</h1>

  <% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>


  <table>
    <thead>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </thead>
    <tbody>
      <tr>
        <td></td>
        <td></td>
        <td>
          <a class="show_id" href="/books/show">Show</a>
        </td>
        <td>
          <a class="edit_id" href="/books/edit">Edit</a>
        </td>
        <td>
          <a data-confirm="Are you sure?" class="destroy_id" rel"nofollow" data_method="delete" href="books/delete">Destroy</a>
        </td>
      </tr>
    </tbody>
  </table>

</body>


<%= form_with model: @book,url:'/books', method: :post do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>

<% end %>

